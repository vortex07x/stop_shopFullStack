# ======================
# Database - Railway will override these with environment variables
# ======================
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3307/ecommdb}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.application.name=ecomm

# Server port for Railway
server.port=${PORT:8080}

# ======================
# JWT Config
# ======================
jwt.secret=${JWT_SECRET:defaultSecretKeyForDevelopmentOnly12345678901234567890}
jwt.expiration=${JWT_EXPIRATION:3600000}

# ======================
# Brevo SMTP - Hardcoded for deployment (since it works for you)
# ======================
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=95fa5e003@smtp-brevo.com
spring.mail.password=qmJR7F5TLdYAHzM1
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.smtp.debug=false

# ======================
# CORS Configuration
# ======================
spring.web.cors.allowed-origins=http://localhost:3000,https://*.netlify.app